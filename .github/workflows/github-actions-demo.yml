name: Test Action Demo
on: 
  pull_request:
    types:
      -  closed
    branches:
      -  main
jobs:
  Test-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "pull request to main branch complete."
